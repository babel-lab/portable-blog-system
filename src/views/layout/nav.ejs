<% if (navigation && navigation.length > 0) { %>
<nav class="lab-nav" aria-label="主選單">
  <% navigation.forEach(function (item) { %>
  <a class="lab-nav__link" href="<%= basePath %><%= item.href %>"><%= item.label %></a>
  <% }); %>
</nav>
<% } %>
