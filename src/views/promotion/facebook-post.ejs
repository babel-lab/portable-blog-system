<%- post.fbTitle || post.title %>

<%- post.message %>

<%- post.finalUrl || '(URL 待設定 site.config.json githubSiteUrl / bloggerSiteUrl)' %>

<%- await include('facebook-hashtags', { hashtags: post.hashtags }) %>
